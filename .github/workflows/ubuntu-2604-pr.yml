name: "Check Ubuntu 26.04"
on:
  pull_request:
    paths:
      - 'ci-ubuntu-2604.dockerfile'
      - 'ubuntu/**'

jobs:
  latest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: make ubuntu-2604
